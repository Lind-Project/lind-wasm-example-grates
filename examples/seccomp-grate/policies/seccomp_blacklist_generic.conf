; this is a generic config for when default
; policy is set to blacklist

[default]
blacklist

; syscalls defined in the whitelist section
; should be present for successful grate
; execution and termination

[whitelist]
SYS_futex
SYS_exec
SYS_mmap
SYS_munmap
SYS_exit
SYS_exit_group

